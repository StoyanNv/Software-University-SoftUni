@page "{id}"
@model BookLibrary.Web.Pages.Authors.DetailsModel
@{
    ViewData["Title"] = "Details";
}

<h2>@Model.Name</h2>
<ul>
    @foreach (var book in Model.BookNames)
    {
        <li><a asp-page="/Books/Details" asp-route-id="@(book.BookId)">@book.Title</a></li>
    }
</ul>